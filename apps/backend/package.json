{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "test": "cargo test",
    "test:coverage": "cargo llvm-cov --lcov --output-path coverage.lcov --fail-under-lines 75 --ignore-filename-regex 'src/main.rs|rustlib'",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "typecheck": "cargo check --all-targets",
    "format:check": "cargo fmt --check",
    "format": "cargo fmt",
    "dev": "cargo run"
  }
}
